/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: '', // Set your basePath if needed. Empty string if the site is served from the root
  assetPrefix: '',
}

module.exports = nextConfig
